class Video {
    constructor(id, uri, title, description, comments, user) {
        this.id = id;
        this.uri = uri;
        this.title = title;
        this.description = description;
        this.comments = comments;
        this.user = user;
    }
}

export default Video;
